# End-to-end audit, 2026-09-06 00:20 to 01:05 IST

Fourteen read-only auditors, one per module, against the working tree at 00:20 IST and the live Maincloud database `echoe`. Every finding carries a file and line so it can be re-checked. Items marked **confirmed** were re-verified by the lead against source or live tables after the auditor reported them. Nothing was edited, published or called during the audit.

Code freeze 08:30 IST. Judges open the live app in two tabs, act in one, watch the other, then read the module.

Not executed: the live two-tab walk in a separate browser. The browser tool hung for the two agents that tried it, so every runtime claim below is from source plus live table reads, not from a watched session.

## Qualifiers (handbook pass/fail)

| # | Finding | Evidence | Fix |
|---|---|---|---|
| Q1 | **Repo is PRIVATE.** Handbook requires judge-accessible code. Created 16:02 IST Saturday, so the fresh-code rule is fine. | `gh repo view` → `visibility: PRIVATE` | Flip to public before submission. |
| Q2 | **A clone of HEAD does not compile.** `src/module_bindings/index.ts:43-75` imports `join_event_reducer.ts`, `leave_event_reducer.ts`, `seed_places_reducer.ts`, `event_join_table.ts`, all untracked. 26 paths uncommitted in total. **confirmed** | `git status --short` | Commit everything before 08:30. |
| Q3 | **Apex `echoe.world` still has the stray Hostinger A record** (2.57.91.91). Round-robin means some judges hit a wrong cert. Passed on this run by luck. | Earlier session's DNS check | Delete the Hostinger A record; publish only `www.echoe.world` in the post. |
| Q4 | **LICENSE is the unedited starter file**: "Copyright 2025 Clockwork Labs, Inc". | `LICENSE` | Put the real holder in. |

## P0: breaks the two-tab demo

### Module, the world tick (`spacetimedb/src/index.ts`)

| # | Finding | Evidence | Fix |
|---|---|---|---|
| T1 | **Conversations starve.** At `:1528` an Echoe that is engaged but not on turn returns `true` and does nothing else that tick, so any other conversation waiting on it never advances. **confirmed live: 14 of 47 conversations never reached 4 exchanges** (ids 17, 20, 23, 27, 29, 30, 32, 33, 34, 38, 39, 42, 43, 44). | `SELECT id, replies FROM conversation WHERE replies < 4` | Keep scanning; act on the open conversation with the lowest `lastExchangeAt` where it is this side's turn; return `true` only after acting. |
| T2 | **Host is never marked met.** `:1570-1577` sets `hostMet` only on the run that opens the conversation. The host has the lower id and always opens, so the visitor's flag stays false and `pickNextPlace` routes it back to the host all night. **confirmed live: all 4 hosted runs (17, 22, 25, 28) show `host_met=false`.** | `SELECT id, host_echo_id, host_met FROM run WHERE host_echo_id > 0` | Set `hostMet` on whichever of the two runs names the other's echo as its host. |
| T3 | **A visitor can never open with an ended host.** `:1530` lets only the lower id open; a share-link host whose run ended never ticks, so a higher-id visitor walks to it forever. | code | Allow the higher id to open when `otherRun === null`. |
| T4 | **Restart wipes the live run.** `startRun` `:1071-1089` upserts over a running row: zeroes `peopleMet`, `placesVisited`, `spentUsd`, `hostMet`, resets `startedAt`. `doneTalking` `:828` then lets the same pair replay a finished conversation, and `:1500` abandons the open ones. **live: three `run_start` receipts inside 90 s for one owner; conversations 42, 43, 44 abandoned at 18:44.** | receipts | If the existing row is running or paused, update only `goal` (and `hostEchoId` when unset). |
| T5 | **Player walks never count.** `travel` `:1036` writes `currentPlace` at departure, so the tick's arrival branch `:1402` never fires: no arrive receipt, `placesVisited` never increments, and a tap after the run ends teleports the pin (`select.ts:186,215`). | code | Drop the `player.identity.update` from `travel`; let the tick bank the arrival as `depart` already does. |
| T6 | **Pause does not stop the clock.** `:1094` and `:1384` compare `now - startedAt` against the 3-minute budget with no pause credit. A judge who pauses to read a transcript gets "Came home: the 24 hours ran out" on resume. Also `travel` has no status gate, so a paused Echoe can still be moved. | code | New table `run_pause {runId, pausedAt}` (new tables need no data wipe); on resume push `startedAt` forward by the paused interval; `fail('run_paused')` in `travel`. |

### Client (`src/`)

| # | Finding | Evidence | Fix |
|---|---|---|---|
| C1 | **Share links do nothing for returning players.** `App.tsx:137-139` bounces anyone with an Echoe from Join to World, so the host card is never shown and no run starts with `hostShareId`. | code | Skip Join only when `!hostShareId`; otherwise show a "Meet <host>" confirm that calls `onStartRun`. |
| C2 | **Run end is missed off the map.** `App.tsx:145-152` navigates to Return only from `roaming` or `world`. A run that ends while on Talks, Limits, Events or Profile does nothing, and a refresh after the end lands on World with no route to the recap. | code | Navigate on the running/paused to ended edge from any screen except Correct; add a recap entry on World when `run.status === 'ended'`. |
| C3 | **Talks and Profile forget everyone from the previous run.** `App.tsx:271-284` feeds `rankedMatches` filtered by `since = myRunRow.startedAt` to both screens. After "Send my Echoe out again" the People count drops to zero. | code | Unfiltered list for Talks and Profile; keep the `since` filter for the Return recap only. |
| C4 | **Real map never moves a standing Echoe.** `BengaluruMap.tsx:1151-1163` caches the leg by `agent.id` and never rebuilds it. A standing Echoe has the stable id `${echoId}-standing` (`select.ts:220`), so "Send my Echoe" updates `currentPlace` while the dot stays at the old landmark. The flat fallback recomputes each frame, so the two maps disagree. **confirmed** | code | Key the cache on `id:fromPlace:toPlace:departMs`. |
| C5 | **Phones without WebGL2 cannot reach the event.** `MapSlot.tsx:58-64` gives the fallback neither `onEventTap` nor the events list; `MapPins` draws landmarks only. **confirmed** | code | Render one chip per event via `nearestLandmark`, wired to the same `onEventTap`. |
| C6 | **Hosting an event can wipe the persona.** The `actions` memo `App.tsx:238-241` omits `myEchoRow`, so `onHostEvent` can send `persona: ''`, and `createEcho` `index.ts:975-979` overwrites unconditionally. **confirmed** | code | Add `myEchoRow?.persona` to the deps; reducer keeps the stored persona when the argument is empty. |
| C7 | **Google connect is absent from production.** `ProfileScreen.tsx:127` returns early when `VITE_GOOGLE_CLIENT_ID` is unset at build time and Rollup drops `google.ts` entirely. **confirmed: the deployed bundle has zero `accounts.google` or `gsi` strings.** Judge sees blank space. | live bundle | Set the var on Vercel and redeploy, or render an explicit "not configured" line. |
| C8 | **The connect command does not exist.** `copy.ts:115` and `connect/README.md:9` tell the judge to run `npx echoe-connect <token>`; the package is a 404 on npm. **confirmed** | `registry.npmjs.org/echoe-connect` → 404 | Publish it, or change to `npx github:Jayanthkoppala/echoe#main/connect`. |
| C9 | **Verify says "Code sent" with no email.** `VerifySheet.tsx:46` ignores the `'logged'` return (`index.ts:2118-2123`, taken whenever `resend_api_key` is unset, which it is: the live `secret` table holds only `admin_identity`). The code is printed to the module log beside the address. Default sender `onboarding@resend.dev` only delivers to the Resend owner anyway. | live `secret` keys | Branch on `'logged'` and say so, or hide the verify sheet for the demo. |

## P0: abuse and privacy, once the URL is public

| # | Finding | Evidence | Fix |
|---|---|---|---|
| S1 | **`agent_memory` is public** (`index.ts:525`). A player's distilled Claude Code / Codex notes are readable by anyone with the CLI, joinable to `player.name`. Currently 0 rows. Two auditors flagged it independently. | `spacetime describe` | Make it private; feed the prompt server-side. Check with a dry publish whether flipping visibility needs a data wipe. |
| S2 | **`correction` is public** (`:352`): 5 rows of "what my Echoe should have said" plus owner identities are readable now. `linked_account` (`:216-222`) publishes `handle` and `hostedDomain`, which rebuilds the work email the comment says never leaves the token. | `SELECT ... FROM correction` | Private tables with owner-scoped views. |
| S3 | **`suggestIntents` spends the house Gemini token with no limit** (`:1949-1987`): client-callable, 2000-char input, no counter, no receipt. | code | Per-identity cooldown plus a global daily counter in a new private table. |
| S4 | **Every cap is per identity and identities are free.** `POST /v1/identity` returns 200 unauthenticated (`connect/sync.js:230`); `FREE_CONVERSATIONS = 5` bounds nothing. N scripted identities buy 5N house conversations. | live | Global cap on concurrent running runs; verified badge or own key after the first free conversation. |
| S5 | **Prompt injection into judge-visible transcripts.** `:1759-1771` splices persona, intent, corrections and agent notes raw into the system prompt, with corrections labelled "obey these over everything", above the format rule at `:1786`. | code | Fence untrusted text as data in the user message; delete the "obey" line; cap persona for the demo. |
| S6 | **`ingestAgentMemory` authenticates on the token argument only** (`:2325`), no rate limit; four calls of twelve junk lines evict all 40 real memories (`:2358-2363`), and those feed the prompt. The paste text (`copy.ts:118`) also puts the token into a Claude Code transcript that `sync.js:186`'s secret filter does not catch. | code | Failure counter and hourly cap per link; add `/\b[0-9a-f]{32}\b/i` to the filter. |
| S7 | **`requestVerification` mails any address** (`:2053`), 3/hour per free identity, codes logged in cleartext (`:2122`). Inert until a Resend key exists. | code | Global hourly counter; or disable for the demo. |

## P0: scale (only matters above ~55 concurrent runs)

| # | Finding | Numbers | Fix |
|---|---|---|---|
| P1 | **Tick crosses the reducer budget.** `pickNextPlace` `:859` scans every `run` row ever written (no status index) and calls `latestLeg` `:786`, which walks the echo's whole travel history. | ~500 ms at 100 live runs / 200 total / 25 legs; 100 ms near 55 runs. An aborted tick freezes everyone. | Status index on `run`; read `otherPlayer.currentPlace` instead of `latestLeg` (the tick already banks it at `:1408`). |
| P2 | **Every tab downloads every row.** `App.tsx:62-65` subscribes to all of `receipt`, `transcript_line`, `agent_travel`; nothing prunes them (`:676`). | 989 receipts to every page load now; 12 MB/client/hour at 100 runs; 12.5 GB egress tier gone in ~10 h; 45 MB snapshot after 12 h × 50 Echoes. | `receipt.where(runOwner = identity)` (index exists); transcript by my conversations; delete a run's legs in `finishRun`. |
| P3 | **Broke Echoes never come home.** `outOfBudget` `:808-816` returns false while any funded conversation is open, and starved conversations (T1) stay open forever. | live | Treat `lastExchangeAt` older than a few ticks as finished for budget. |
| P4 | **`peopleMet` overcounts.** `:1576` credits the other side at insert even when it never speaks. | 10 live conversations have 2 lines yet both runs credited; runs 21, 22 show 6 against 5 free. | Credit when that side first speaks. |

## P1: judge-visible

**LLM output** (`spacetimedb/src/llm.ts`, `index.ts`). Logs show zero LLM errors or fallbacks tonight and the talk queue is empty, so the lane works.
- `llm.ts:224` + `index.ts:1855`: a reply with both turns on one physical line splits to one element and the whole reply is thrown away for a canned line. Also split on `/\s+B:\s*/`.
- `llm.ts:224`: the `\w+:` branch strips the first word of any line like "Honestly: I am hiring". Keep only `^[AB]:\s*`.
- `index.ts:1177-1180`: `behaviourNotes` grows ~1 KB per correction with no cap or dedupe per line and rides every prompt. Cap like `agentMemory` at `:2358`.
- `llm.ts:98`: Google cost hardcoded to 0, so every receipt and `spent_usd` reads zero on the house lane. Price `totalTokenCount` or drop the money framing.
- `index.ts:1819,1663`: fallback and model lines are indistinguishable. Flag fallback rows.
- `index.ts:1622`: the fallback draws from 4 openers at random, so a no-key demo can repeat a line within one conversation.

**Delivery** (Vercel).
- `vercel.json`: hashed assets served `max-age=0, must-revalidate`, same as `index.html`. Add `Cache-Control: public, max-age=31536000, immutable` for `/assets/*`.
- No code splitting: one 1.9 MB chunk (471 KB gzip) plus a 485 KB MapLibre worker, with `routes.json` (315 KB minified) and `companies-osm.json` inlined before the join screen paints. Lazy-import the map screen.
- `JoinScreen.tsx:25`: the 3.7 MB hero video autoplays on the first screen with no `poster` or `preload="metadata"`. First thing a judge on cellular waits on.

**Map** (`src/map/BengaluruMap.tsx`).
- `App.tsx:351,380`: World and Roaming each mount their own `MapSlot`, so every switch destroys and rebuilds the map (style refetch, 3 sources, 8 layers, every chip re-rasterised). Hoist one slot above the switch.
- `:747`: no `map.on('error')` or `webglcontextlost` handler; a tile-host failure leaves a black canvas with floating markers and never falls back.
- `:1210-1213`: follow mode is cancelled by drag, wheel, pitch, rotate but not zoom, so a pinch-zoom snaps back. Add `zoomstart` and `touchstart`.
- `:629`: `fanCoincidentAgents` is O(n²) haversine inside the 30 Hz loop; 100 agents ≈ 148k trig calls/s on the main thread. Bucket by rounded lat/lng.

**Onboarding**.
- `LimitsScreen.tsx:32-36`: goal input has no `maxLength`; module rejects over 280 and the toast shows `Start run failed: goal_too_long:280`.
- `CreateScreen.tsx:59-91` + `styles.css:1320`: opening the "generate one" prompt card may push the intent box out of view on 390×844 since only the outer container clips. Unverified without a device.
- `index.ts:569` → `App.tsx:110`: every machine code reaches the toast verbatim (`already_ended`, `not_running:ended`, `host_intent_expired`). Map codes to sentences in the `run` helper.

**Docs the judges will read**.
- `docs/EVENTS.md:4-8` claims 39 events; `events.json` has 1.
- `docs/DATA-MODEL.md:28` says ten landmarks (eleven); `:26-41` omits `intent`, `event_join`, `agent_link`, `agent_memory`.
- `README.md:20` says 45 routes (55).
- `docs/SPOTS.md` documents a pubs layer whose files no longer exist.
- `docs/SOCIAL-CONNECT.md:284,300` runbook targets database `echo` without `--no-config`; the database is `echoe`.
- `docs/UX-ORDER.md:13,16,66,79,104` specifies a Character row on Create that does not exist.

## P2: hygiene

- `index.ts:1230`: `secret.admin_identity` is claimed by whoever writes first; `init` never seeds it, and `seedPlaces` / `seedCompanies` route through the same gate. Seed in `init`, fail closed.
- `index.ts:789,1707,2359` and `App.tsx:300`: ordering by autoInc id, which `CLAUDE.md` rule 4 forbids; every row has a timestamp.
- `index.ts:325`: `conversation.echoB` has no index; the tick full-scans `conversation.iter()` at `:812-814`.
- `index.ts:36,909`: the tick never idles: 17,280 calls/day at zero players, which also blocks Maincloud's one-week idle pause. Delete `world_tick` when nothing runs; re-insert in `startRun`.
- `index.ts:1278-1290`: no in-flight guard on the token refresh; concurrent `echoTalk` calls all refresh on a cold cache.
- `index.ts:1923`: raw OpenRouter body reaches the browser inside the error.
- `index.ts:969,929`: `createEcho` slices persona silently at 2000; `join` slices email to 120 before validating, so a long address reports `email_invalid`.
- `index.ts:2344`: agent-memory note loop walks every line of an unbounded argument.
- `App.tsx:460-461`: Events back target reuses `profileFrom`; World → Events → back can land on Talks. `ReviewScreen.tsx:111` hardcodes `go('return')` although `backTo` exists.
- `App.tsx:218` + `openrouter.ts:12-13`: `void startOpenRouterLink()` drops the rejection; on a plain-http origin `crypto.subtle` is undefined and the button does nothing.
- `main.tsx:21-23`: `connectionError` is never read; a rejected token shows "Connecting…" forever with Logout only reachable through Profile.
- `session.ts:6-7`: falls back to `ws://localhost:3000` when env is missing; a bad Vercel build dials localhost silently. Throw at load in production.
- `events.json` `endDate` is never read; on Sunday the card reads "Sat 5 Sep".
- `styles.css:1026-1040`: profile button is 34 px, under the 44 px tap target the rest of the UI enforces. `:root` lacks `color-scheme: dark`. `index.html:8` preconnects to `fonts.googleapis.com` but not `fonts.gstatic.com`.
- `MapPins.tsx:8`: `Math.max(0, findIndex)` draws an unknown place on Bangalore Palace. `BengaluruMap.tsx:1162`: `legsRef` grows without bound. `interpolate.ts:45`: linear scan of up to 656 points per agent per frame.
- `VerifySheet.tsx:10`: error map keys `invalid_email`; the module raises `email_invalid`.
- `connect/sync.js:189`: `parseNotes` keeps the model's preamble ("Here are the bullets:") as a fact and `test.js:98` asserts it.
- `CreateScreen.tsx:133-137`, `LimitsScreen.tsx:60-70`: no busy state on the primary CTA; a double-tap fires the chain twice.

## Suggested order before 08:30, smallest diff first

1. Commit everything, make the repo public, fix LICENSE, delete the Hostinger A record. (Q1 to Q4)
2. Tick: T1 (do not return on not-my-turn), T2 (set `hostMet` on both runs), T3 (higher id may open when `otherRun` is null). Three small edits in one function, then republish and re-run the 47-conversation invariant.
3. T4 `startRun` guard on a live run, T5 drop the `currentPlace` write in `travel`. Two edits.
4. Client: C1, C2, C3, C6 are each one to five lines in `App.tsx`. C4 is a three-line cache key.
5. Privacy: S1 and S2 table visibility, S5 prompt reorder. Dry-publish first to learn whether visibility flips need a wipe.
6. P2 subscription narrowing in `App.tsx:62,64`, then P1 `pickNextPlace` on `currentPlace`.
7. C7 and C8 copy: explicit "not configured" line and the GitHub connect command. C9 hide verify for the demo.
8. Delivery: `vercel.json` cache headers, video `poster` and `preload`.
9. Docs: the six drift items.

Left for after the event: T6 pause clock (needs a new table), S3, S4, S6, S7 abuse caps, map remount hoisting, code splitting, LLM split regex, everything in P2.

## Checks the auditors ran

`spacetime build` pass. `tsc --noEmit` pass. `vite build` pass at 1,913,036 B JS. `connect/test.js` 10/10 pass. Live: 25 tables, 27 reducers, 6 procedures; `talk_job` empty; no LLM errors in 300 log lines; `place` has 11 rows including the*spark; all three hostnames HTTP 200 with valid TLS from this machine at 01:00 IST.

Not verified anywhere: two-tab propagation delay, `player.online` across two tabs sharing one identity, real-device rendering at 390×844, first paint on a mid-range Android, anything under load. The `secret` table's key columns were never selected.

## Addendum 01:30 IST: judge-walk results and concurrent edits

The judge-walk auditor's browser never responded (two attempts, 1800 s each), so it did the static and table checks only. New items from it, verified by the lead:

- **Solo judge meets nobody.** At 01:26 IST the `run` table has 0 running runs out of 29. Runs last 3 minutes, so unless someone else is live at judging time a judge's Echoe walks alone for the whole window. Treat as P0 for the demo. Fix: seed a few resident Echoes with house-funded runs that restart themselves in the tick, or keep two of your own tabs running from 08:00.
- **Match score is computed once, at conversation creation.** `matchPair` is called at insert time only, never after an exchange, so a fresh pair that scored "no overlap yet" keeps score 0 after four coherent lines. Judges reading the `conversation` table see the matcher doing nothing. P1. Fix: rescore when the conversation closes.
- **`suggest_intents` "nonexistent reducer" error at 00:30 IST.** Verified in the log once. Now moot: the procedure and its client call were removed from the working tree after the audit began (see below). S3 above is resolved by the same removal.
- Repo private and deployed build behind the working tree: both already covered by Q1 and Q2.

**Concurrent edits.** `spacetimedb/src/index.ts` was modified at 01:25:54 IST while this report was being written: 378 insertions, a new "Mutual reveal" section, and the intent-suggestion section gone. `App.tsx`, `select.ts`, `types.ts`, `copy.ts`, `styles.css` and four screens changed in the same window. Line numbers in this report are against the tree at 00:20 IST. For the module, everything after the Tables section has shifted; the section headers now sit at: Helpers 633, Lifecycle 1064, join/create 1106, live world 1210, limits/roaming 1241, review/correct 1325, Mutual reveal 1387, LLM config 1472, world tick 1632, deterministic dialogue 1906, LLM procedure 1954. Each finding names its function, so search by name rather than line.
